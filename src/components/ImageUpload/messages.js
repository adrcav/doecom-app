import { defineMessage } from 'react-intl';

const messages = {
  noImage: {
    id: 'app.imageUpload.noImage',
    defaultMessage: 'No Image'
  },

  change: {
    id: 'app.imageUpload.change',
    defaultMessage: 'Edit'
  },

  clickToSelect: {
    id: 'app.imageUpload.clickToSelect',
    defaultMessage: 'Click to select a image'
  }
};

export default defineMessage(messages);
